<!--@param ${test.machine.deploy.path} , where test cases in the test machine-->
<!--@param ${test.machine.deploy.path} , where deploy file in the test machine-->

<!--@param ${antFileName} , the ant file name is -->
<!--@param ${test.reports.path} , where the report put in-->
<!--@param ${Test-Type} , what the test type-->
<!--@param ${WORKSPACE}: where the working space is it-->
<!--@param ${ci.script.file.path} : where the ci.test.properties file is it -->
<!--@${test.host.NAME}:which machine test testcases under unparallel env -->

<project name="Test-Type" default="Test-Type">
	<target name="Test-Type" depends="">   
		<property file="${basedir}/ci.test.properties" />		
		<switch value="${Test-Type}">
		   
			<case value="story">
			   <echo message="... Test story testcases ..." />
			   <property file="${basedir}/testAllot.conf" />
				<for list="${machineList}" param="hostname" parallel="true">
					<sequential>
						<sdbtest host="@{hostname}" testscript="${test.machine.deploy.path}/deploy/exec-basic-testcases.xml" remotereports="${testcase.path}/js_testcases/reports" masterreports="${test.reports.path}" >
							<param name="basic.testcase.path" value="${testcase.path}/js_testcases" />
							<param name="excludes.dir.patterns" value="vote" />
							<param name="includes.dir.patterns" value="${@{hostname}}" />
							<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
							<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
							<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
							<param name="WORKSPACE" value="${WORKSPACE}" />
							<param name="ci.script.file.path" value="${ci.script.file.path}" />
							<param name="test.log" value="${test.log}" />
							<param name="test-type-list" value="${Test-Type}" />
							<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
						</sdbtest>
					</sequential>
				</for>
				<echo message="... Test story testcases is over ..." />
			</case>
            
         <case value="sdv">
            <echo message="... Test sdv testcases ..." />
            <property file="${basedir}/testAllot-sdv.conf" />
				<for list="${machineList}" param="hostname" parallel="true">
					<sequential>
						<sdbtest host="@{hostname}" testscript="${test.machine.deploy.path}/deploy/exec-sdv-testcases.xml" remotereports="${testcase.path}/sdv_testcases/reports" masterreports="${test.reports.path}" >
							<param name="testcase.path" value="${testcase.path}/sdv_testcases" />
							<param name="excludes.dir.patterns" value="" />
							<param name="includes.dir.patterns" value="${@{hostname}}" />						
							<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
							<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
							<param name="ci.properties.file" value="${test.machine.software.path}/ci.test.properties" />
							<param name="WORKSPACE" value="${WORKSPACE}" />
							<param name="ci.script.file.path" value="${ci.script.file.path}" />
							<param name="test.log" value="${test.log}" />
							<param name="test.type" value="${Test-Type}" />
							<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
						</sdbtest>
					</sequential>
				</for>
				<echo message="... Test sdv testcases is over ..." />
			</case>
			
			<case value="ant">
				<echo message="... Test ant testcases ..." />
				<sdbtest host="${test.host.NAME}" testscript="${test.machine.deploy.path}/deploy/exec-ant-testcases.xml" remotereports="${testcase.path}/ant_testcases/reports" masterreports="${test.reports.path}">
					<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
					<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
					<param name="ant.testcase.path" value="${testcase.path}/ant_testcases" />
					<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<param name="excludes.dir.patterns" value="reports*" />
					<param name="includes.dir.patterns" value="*" />
					<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
					<param name="test-type-list" value="${Test-Type}" />
				</sdbtest>
				<echo message="... Test ant testcases is over ..." />
			</case>

			<case value="driver-python">
				<echo message="...Test python driver..."/>
				<sdbtest host="${test.host.NAME}" testscript="${test.machine.deploy.path}/deploy/exec-driver-python-test.xml" remotereports="${testcase.path}/drivers_testcases/python/reports" masterreports="${test.reports.path}">
					<param name="test.host.NAME" value="${test.host.NAME}" />
					<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
					<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
					<param name="python.testcase.path" value="${testcase.path}/drivers_testcases/python" />
					<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<param name="excludes.dir.patterns" value="reports*" />
					<param name="includes.dir.patterns" value="*" />
					<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
					<param name="test-type-list" value="${Test-Type}" />
				</sdbtest>
				<echo message="... Test python driver is over ..." />
			</case>

			<case value="driver-php" >
				<echo message="... Test php driver ..." />
				<sdbtest host="${test.host.NAME}" testscript="${test.machine.deploy.path}/deploy/exec-driver-php-test.xml" remotereports="${testcase.path}/drivers_testcases/php/reports" masterreports="${test.reports.path}" >
					<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
					<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
					<param name="php.testcase.path" value="${testcase.path}/drivers_testcases/php" />
					<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<param name="excludes.dir.patterns" value="tools,SequoiaGroup,SequoiaNode" />
					<param name="includes.dir.patterns" value="*" />
					<param name="php-bin.dir" value="/opt/sequoiadb/tools/server/php/bin" />
					<param name="phpunit.phar.dir" value="${testcase.path}/drivers_testcases/php/tools/phpunit.phar" />
					<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
					<param name="test-type-list" value="${Test-Type}" />
				</sdbtest>
				<echo message="... Test php driver is over ..." />
			</case>
			
			<case value="story-php" >
				<echo message="... Test story php testcases ..." />
				<sdbtest host="${test.host.NAME}" testscript="${test.machine.deploy.path}/deploy/exec-story-php-test.xml" 
				   remotereports="${testcase.path}/drivers_testcases/story_php/reports" masterreports="${test.reports.path}" >
					<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
					<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
					<param name="php.testcase.path" value="${testcase.path}/drivers_testcases/story_php" />
					<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<param name="excludes.dir.patterns" value="tools,cluster,sdb_ci_not_run" />
					<param name="includes.dir.patterns" value="*" />
					<param name="php-bin.dir" value="/opt/sequoiadb/tools/server/php/bin" />
					<param name="phpunit.phar.dir" value="${testcase.path}/drivers_testcases/story_php/tools/phpunit.phar" />
					<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
					<param name="test-type-list" value="${Test-Type}" />
				</sdbtest>
				<echo message="... Test story php testcases is over ..." />
			</case>
 
			<case value="driver-java">
				<echo message="... Test java driver ..." />
				<sdbtest host="${test.host.NAME}" testscript="${test.machine.deploy.path}/deploy/exec-driver-java-test.xml" remotereports="${testcase.path}/drivers_testcases/java/reports" masterreports="${test.reports.path}" >
					<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
					<param name="java.testcase.path" value="${testcase.path}/drivers_testcases/java" />
					<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<param name="includes.dir.patterns" value="*" />
					<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
					<param name="test-type-list" value="${Test-Type}" />
					<param name="isTrunk" value="${isTrunk}" />
				</sdbtest>
				<echo message="... Test java driver is over ..." />
			</case>

			<case value="story-java">
				<echo message="... Test story java ..." />
				<sdbtest host="${test.host.NAME}" testscript="${test.machine.deploy.path}/deploy/exec-story-java-test.xml" 
				   remotereports="${testcase.path}/drivers_testcases/story_java/reports" masterreports="${test.reports.path}" >
					<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
					<param name="java.testcase.path" value="${testcase.path}/drivers_testcases/story_java" />
					<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<param name="includes.dir.patterns" value="*" />
					<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
					<param name="test-type-list" value="${Test-Type}" />
					<param name="isTrunk" value="${isTrunk}" />
				</sdbtest>
				<echo message="... Test story java is over ..." />
			</case>
			
			<case value="lob">
				<echo message="... Test lob testcases ..." />
				<sdbtest host="${test.host.NAME}" testscript="${test.machine.deploy.path}/deploy/exec-lob-testcases.xml" remotereports="${testcase.path}/lob_testcases/reports" masterreports="${test.reports.path}" >
					<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
					<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
					<param name="lob.testcase.path" value="${testcase.path}/lob_testcases/build" />
					<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<param name="excludes.dir.patterns" value="" />
					<param name="includes.dir.patterns" value="*" />
					<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
					<param name="test-type-list" value="${Test-Type}" />
				</sdbtest>
				<echo message="... Test lob testcases is over ..." />
			</case>

			<case value="driver-C">
				<echo message="... Test c driver ..." />
				<sdbtest host="${test.host.NAME}" testscript="${test.machine.deploy.path}/deploy/exec-driver-C-test.xml" remotereports="${testcase.path}/drivers_testcases/C/reports" masterreports="${test.reports.path}" >
					<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
					<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
					<param name="C.testcase.path" value="${testcase.path}/drivers_testcases/C/build" />
					<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<param name="excludes.dir.patterns" value="" />
					<param name="includes.dir.patterns" value="*" />
					<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
					<param name="test-type-list" value="${Test-Type}" />
				</sdbtest>
				<echo message="... Test c driver is over ..." />
			</case>

			<case value="driver-CPP">
				<echo message="... Test c++ driver ..." />
				<sdbtest host="${test.host.NAME}" testscript="${test.machine.deploy.path}/deploy/exec-driver-CPP-test.xml" remotereports="${testcase.path}/drivers_testcases/CPP/reports" masterreports="${test.reports.path}" >
					<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
					<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
					<param name="CPP.testcase.path" value="${testcase.path}/drivers_testcases/CPP/build" />
					<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<param name="excludes.dir.patterns" value="rg" />
					<param name="includes.dir.patterns" value="*" />
					<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
					<param name="test-type-list" value="${Test-Type}" />
				</sdbtest>
				<echo message="... Test c++ driver is over ..." />
			</case>
			
			<case value="story-C">
				<echo message="... Test story c ..." />
				<sdbtest host="${test.host.NAME}" testscript="${test.machine.deploy.path}/deploy/exec-story-C-test.xml" 
				   remotereports="${testcase.path}/drivers_testcases/story_C/reports" masterreports="${test.reports.path}" >
					<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
					<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
					<param name="C.testcase.path" value="${testcase.path}/drivers_testcases/story_C/build" />
					<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<param name="excludes.dir.patterns" value="" />
					<param name="includes.dir.patterns" value="*" />
					<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
					<param name="test-type-list" value="${Test-Type}" />
				</sdbtest>
				<echo message="... Test story c is over ..." />
			</case>

			<case value="story-CPP">
				<echo message="... Test story c++ ..." />
				<sdbtest host="${test.host.NAME}" testscript="${test.machine.deploy.path}/deploy/exec-story-CPP-test.xml" 
				   remotereports="${testcase.path}/drivers_testcases/story_CPP/reports" masterreports="${test.reports.path}" >
					<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
					<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
					<param name="CPP.testcase.path" value="${testcase.path}/drivers_testcases/story_CPP/build" />
					<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<param name="excludes.dir.patterns" value="rg" />
					<param name="includes.dir.patterns" value="*" />
					<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
					<param name="test-type-list" value="${Test-Type}" />
				</sdbtest>
				<echo message="... Test story c++ is over ..." />
			</case>			
			
			<case value="driver-mongoc">
				<echo message="...Test mongoc driver..." />     
				<sdbtest host="${test.host.NAME}" testscript="${test.machine.deploy.path}/deploy/exec-driver-mongoc-test.xml" remotereports="${testcase.path}/drivers_testcases/mongoc/reports" masterreports="${test.reports.path}" >
					<param name="ci.properties.path" value="${test.machine.software.path}/ci.test.properties" />
					<param name="antTask.jar.path" value="${test.machine.deploy.path}/deploy/ant-sdbtasks" />
					<param name="mongoc.testcase.path" value="${testcase.path}/drivers_testcases/mongoc/build" />
					<param name="mongoc.testcase.libpath" value="${testcase.path}/drivers_testcases/mongoc/lib" />
					<param name="mongoc.test.host" value="localhost:${fap.service.port}" />
					<param name="deploy.case.fullname" value="${deploy.case.fullname}" />
					<param name="BUILD_NUMBER" value="${BUILD_NUMBER}" />
					<param name="test-type-list" value="${Test-Type}" />
				</sdbtest>
				<echo message="... Test mongoc driver is over ..." />
			</case>
						
		</switch>
	</target>
</project>
