<!--@param ${basic.testcase.path}: The path where test case direcotry.-->

<!--@param ${antTask.jar.path}: The antTask.jar's path.-->
<!--@param ${reports.path}: The path where test case direcotry.-->
<!--@param ${test.package.name}: The name of test package name,.-->
<!--@param ${parallel.num}:  The parallel number when parallel in same host.-->
<!--@param ${excludes.dir.patterns}: A comma- or space-sparated list of patterns of directories that must be excluded.-->
<!--@param ${ci.properties.path}: where is the ci.properties file -->
<!--@param ${report.suffix}:this is for long time test, default is NULL-->

<!--@param ${WORKSPACE}: where the working space is it-->
<!--@param ${ci.script.file.path} : where the ci.test.properties file is it -->

<project name="Exec-Basic-Testcases" default="Test-Basic-Testcases">
	<hostinfo prefix="host" />
	<property file="${ci.properties.path}" />
	<property name="bin_dir" value="${install.path}/sequoiadb/bin" />
	<taskdef resource="com/sequoiadb/ant/ant-sdbtasks.xml">
		<classpath>
			<fileset dir="${antTask.jar.path}" includes="*.jar" />
		</classpath>
	</taskdef>

	<target name="Clean-Basic-Reports" depends="">
		<echo message="Reports' path is ${reports.path}" />
		<delete dir="${reports.path}" failonerror="false" />
		<mkdir dir="${reports.path}" />
		<echo message="Clean deploy result reports compeleted." />
	</target>

	<target name="Test-Basic-Testcases" depends="Clean-Basic-Reports">
		<echo message="Begin test basic testcases..." />
		<echo message="Perform the basic testcases,with testcase.path=${basic.testcase.path}/js" />

		<tstamp>
			<format property="test_js_begintime" pattern="yyyy-MM-dd-hh.mm.ss a" />
		</tstamp>
		
		<exec executable="${bin_dir}/sdb" dir="" timeout="20000" failonerror="false">
			<arg line="-s &quot;var db = new Sdb('${host.NAME}', '${coord.service.port}');&quot;" />
		</exec>

		<exec command="/bin/sh -c 'eval echo $$$' " dir="" timeout="900000" outputproperty="pid" />
		<sdbprefix prefixName="CHANGEDSTRING">
			<param name="${begin.flag}" value="Test_" />
			<param name="${host.NAME}" value="${host.NAME}_" />
			<param name="${pid}" value="${pid}_" />
			<param name="${parallel.num}" value="${parallel.num}_" />
			<param name="${end.flag}" value="_end" />
		</sdbprefix>

		<property name="sdb.cmd.allPrepare" value=" -e &quot;var CHANGEDPREFIX='${CHANGEDSTRING}'; var COORDSVCNAME=${coord.service.port};&quot; -f &quot;${basic.testcase.path}/libs/func.js,${basic.testcase.path}/libs/all_prepare.js&quot;" />
		<echo message="Exec:sdb ${sdb.cmd.allPrepare}" />
		<exec executable="${bin_dir}/sdb" dir="" timeout="100000" outputproperty="allPrepare.screen.output"  resultproperty="allPrepare.ret.code">
			<arg line="${sdb.cmd.allPrepare}" />
		</exec>
		<echo message="${allPrepare.screen.output}" />

		<foreach target="test-one-suite" param="test.suite.fullname" >
			<path>
				<dirset dir="${basic.testcase.path}/js" includes="${includes.dir.patterns}" excludes="${excludes.dir.patterns}" />
			</path>
			<param name="testcases.path" value="${basic.testcase.path}" />
			<param name="test-type" value="${test-type-list}" />
			<param name="includes.case.patterns" value="*.js" />
			<param name="excludes.case.patterns" value="commlib.js,split_killDataGroup.js" />
			<param name="CHANGEDSTRING" value="${CHANGEDSTRING}" />
		</foreach>

		<property name="sdb.cmd.allClean" value=" -e &quot;var CHANGEDPREFIX='${CHANGEDSTRING}'; var COORDSVCNAME=${coord.service.port};&quot; -f &quot;${basic.testcase.path}/libs/func.js,${basic.testcase.path}/libs/all_clean.js&quot;" />
		<echo message="Exec:sdb ${sdb.cmd.allClean}" />
		<exec executable="${bin_dir}/sdb" dir="" timeout="100000" outputproperty="allClean.screen.output" resultproperty="allClean.ret.code">
			<arg line="${sdb.cmd.allClean}" />
		</exec>
		<echo message="${allClean.screen.output}" />
		
		<exec executable="${bin_dir}/sdb" dir="" timeout="20000" failonerror="false">
			<arg line="-s &quot;db.close();&quot;" />
		</exec>		

		<tstamp>
			<format property="test_js_endtime" pattern="yyyy-MM-dd-hh.mm.ss a" />
		</tstamp>
		<sdbTime beginTime="${test_js_begintime}" endTime="${test_js_endtime}" output="test_js_time" />
		<echo message="**************************************************************************************************************************************************************" />
		<echo level="info" message="The testsuite began at &lt;${test_js_begintime}&gt;, end at &lt;${test_js_endtime}&gt;,total spending ${test_js_time}s." />
		<echo message="**************************************************************************************************************************************************************" />
	</target>

	<target name="test-one-suite" depends="">
		<basename property="test.suite.name" file="${test.suite.fullname}" />
		<basename property="deploy.name" file="${deploy.case.fullname}" suffix=".xml" />
		<property name="test.suite.class.name" value="testcases-${test-type}.${deploy.name}-${host.NAME}-${parallel.num}-${test.suite.name}" />

		<tstamp>
			<format property="testsuite_begintime" pattern="yyyy-MM-dd-hh.mm.ss a" />
		</tstamp>

		<echo message="Now testsuite ${test.suite.name} ..." />
		<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" message="&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;&#10;&lt;testsuite name=&quot;${test.suite.name}&quot; type=&quot;${test-type}&quot; deploy=&quot;${deploy.name}&quot; hostname=&quot;${host.NAME}&quot; tests=&quot;1&quot; failure=&quot;0&quot;&gt;&#10;" />

		<available property="commlib.exist" file="${test.suite.fullname}/commlib.js" />
		<if>
			<isset property="commlib.exist" />
			<then>
				<echo message="${test.suite.fullname}/commlib is exist , then test will add it " />
				<sdbProperty name="commlib.option" value="${test.suite.fullname}/commlib.js" />
			</then>
			<else>
				<echo message="${test.suite.fullname}/commlib is not exist , then test will not add it" />
				<sdbProperty name="commlib.option" value="" />
			</else>
		</if>

		<available property="basic_testcases_exist" file="${test.suite.fullname}/basic_testcases.list" />
		<if>
		   	<equals arg1="${test-type-list}" arg2="basic" />
			<then>
				<if>
					<equals arg1="${basic_testcases_exist}" arg2="true" />
					<then>
						<property file="${test.suite.fullname}/basic_testcases.list" />
						<echo message="**************************************************************************************************************************************************************" />
						<echo message="basic_list=${basic_list}" />
						<var name="includes.case.patterns" value="${basic_list}" />
						<echo message="includes.case.patterns=${basic_list}" />
						<echo message="**************************************************************************************************************************************************************" />
					</then>
					<else>
						<echo message="**************************************************************************************************************************************************************" />
						<var name="includes.case.patterns" value="" />
						<echo message="includes.case.patterns=${includes.case.patterns}" />
						<var name="excludes.case.patterns" value="*" />
						<echo message="excludes.case.patterns=${excludes.case.patterns}" />
						<echo message="**************************************************************************************************************************************************************" />
					</else>
				</if>	
			</then>
		</if>
		<foreach target="test-one-case" param="test.case.fullname">
			<fileset dir="${test.suite.fullname}" includes="${includes.case.patterns}" excludes="${excludes.case.patterns}" />
			<param name="testcases.path" value="${testcases.path}" />
			<param name="test-type" value="${test-type}" />
			<param name="test.suite.class.name" value="${test.suite.class.name}" />

			<param name="host.NAME" value="${host.NAME}" />
			<param name="commlib.option" value="${commlib.option}" />
			<param name="test.suite.name" value="${test.suite.name}" />
			<param name="deploy.name" value="${deploy.name}" />
			<param name="CHANGEDSTRING" value="${CHANGEDSTRING}" />
		</foreach>

		<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" message="&lt;/testsuite&gt;&#10;" append="true" />
		<tstamp>
			<format property="testsuite_endtime" pattern="yyyy-MM-dd-hh.mm.ss a" />
		</tstamp>
		<sdbTime beginTime="${testsuite_begintime}" endTime="${testsuite_endtime}" output="testsuite_time" />
		<echo message="**************************************************************************************************************************************************************" />
		<echo level="info" message="The testsuite began at &lt;${testsuite_begintime}&gt;, end at &lt;${testsuite_endtime}&gt;,total spending ${testsuite_time}s." />
		<echo message="Now testsuite ${test.suite.name} ..." />
		<echo file="/opt/testsuite.log" append="true" message="Now testsuite ${test.suite.name},cost ${testsuite_time}s,hostname=${host.NAME}&#10;" />
		<echo message="**************************************************************************************************************************************************************" />
	</target>

	<target name="test-one-case" depends="">
	    <echo message="********************************************"/>
		<exec executable="${bin_dir}/sdb" timeout="100000" dir="" outputproperty="SDB.VERSION.INFO" resultproperty="ret.sdb.version.exec">
			<arg line=" --version" />
		</exec>
		<if>
			<equals arg1="${os.arch}" arg2="ppc64" />
			<then>
			  	<property name="URL.download.pre" value="http://192.168.30.150:8080" />
			</then>
			<else>
				<property name="URL.download.pre" value="http://192.168.30.184:8080" />
			</else>
		</if>

		<tstamp>
			<format property="testcase.begin.time" pattern="yyyy-MM-dd-hh.mm.ss a" />
		</tstamp>

		<basename property="test.case.name" file="${test.case.fullname}" suffix=".js" />
		<dirname property="testcase.dirname" file="${test.case.fullname}" />
		
		<exec executable="sed">
			<arg line="-i 's/^\xEF\xBB\xBF//g'  ${test.case.fullname}"/>
		</exec>
		<exec executable="sed">
			<arg line="-i 's/^\xEF\xBB\xBF//g'  ${testcases.path}/libs/func.js"/>
		</exec>
		<echo message="Now testcase ${test.case.name}.js begin with ${testcase.begin.time}"/>

		<property name="sdb.cmd.beforeTestcase" value=" -e &quot;var CHANGEDPREFIX='${CHANGEDSTRING}'; var COORDSVCNAME='${coord.service.port}' ;&quot; -f &quot;${testcases.path}/libs/func.js,${testcases.path}/libs/before_usecase.js&quot;" />
		<echo message="Exec:sdb${sdb.cmd.beforeTestcase}" />
		<exec executable="${bin_dir}/sdb" dir="" timeout="900000" outputproperty="beforeTestcase.screen.output" resultproperty="beforeTestcase.ret.code">
			<arg line="${sdb.cmd.beforeTestcase}" />
		</exec>
		<echo message="${beforeTestcase.screen.output}" />

      <exec executable="${bin_dir}/sdb" dir="" timeout="20000" failonerror="false">
			<arg line="-s &quot;db.msg('Begin testcase[${test.case.fullname}]');&quot;" />
		</exec>
		
		<echo message="--------------------begin testcase----------------------------"/>
		<property name="sdb.cmd.argument" value=" -e &quot;var CHANGEDPREFIX='${CHANGEDSTRING}';var COORDSVCNAME='${coord.service.port}';var TESTCASEDIR='${testcase.dirname}';&quot; -f &quot;${testcases.path}/libs/func.js,${commlib.option},${test.case.fullname}&quot;" />
		<echo message="Exec: ${bin_dir}/sdb${sdb.cmd.argument}" />				
		<exec executable="${bin_dir}/sdb" dir="" timeout="3600000" outputproperty="screen.output" resultproperty="ret.code" failonerror="false">
			<arg line="${sdb.cmd.argument}" />
		</exec>		
		<echo message="${screen.output}" />
		<echo message="--------------------end testcase----------------------------"/>
		
		<exec executable="${bin_dir}/sdb" dir="" timeout="20000" failonerror="false">
			<arg line="-s &quot;db.msg('End testcase[${test.case.fullname}]');&quot;" />
		</exec>

		<property name="sdb.cmd.afterTestcase" value=" -e &quot;var CHANGEDPREFIX='${CHANGEDSTRING}'; var COORDSVCNAME='${coord.service.port}' ;&quot; -f &quot;${testcases.path}/libs/func.js,${testcases.path}/libs/after_usecase.js&quot;" />
		<echo message="Exec:sdb ${sdb.cmd.afterTestcase}" />
		<exec executable="${bin_dir}/sdb" dir="" timeout="900000" outputproperty="afterTestcase.screen.output" resultproperty="afterTestcase.ret.code">
			<arg line="${sdb.cmd.afterTestcase}" />
		</exec>
		<echo message="${afterTestcase.screen.output}" />

		<if>
			<not>
				<equals arg1="${ret.code}" arg2="0" />
			</not>
			<then>
				<tstamp>
					<format property="testcase.failure.end.time" pattern="yyyy-MM-dd-hh.mm.ss a" />
				</tstamp>
				<sdbTime beginTime="${testcase.begin.time}" endTime="${testcase.failure.end.time}" output="testcase.failure.timeOutput" />
				<echo message="The result code is ${ret.code} and it's fail ..." />
				<echo message="The testcase ${test.case.name}.js end with ${testcase.failure.end.time} and cost ${testcase.failure.timeOutput} s in total..." />

				<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" append="true" message="&lt;testcase classname=&quot;${test.suite.class.name}&quot; name=&quot;${test.case.name}&quot; begin_time=&quot;${testcase.begin.time}&quot; end_time=&quot;${testcase.failure.end.time}&quot; time=&quot;${testcase.failure.timeOutput}&quot; path=&quot;${testcases.path}&quot; status=&quot;fail&quot;&gt;&#10;&lt;failure&gt;&#10;&lt;![CDATA[&#10;Exec: ${bin_dir}/sdb${sdb.cmd.argument}&#10;RETURNCODE : ${ret.code}&#10;SDB VERSION : &#10; ${SDB.VERSION.INFO}&#10;&#10;ERROR MESSAGE : &#10;${screen.output}&#10;&#10;Download the test log :&#10;${URL.download.pre}/${test.log}.tar.gz]]&gt;&#10;&lt;/failure&gt;&#10;&lt;/testcase&gt;&#10;" />
			</then>
			<else>
				<tstamp>
					<format property="testcase.end.time" pattern="yyyy-MM-dd-hh.mm.ss a" />
				</tstamp>
				<sdbTime beginTime="${testcase.begin.time}" endTime="${testcase.end.time}" output="testcase.timeOutput" />
				<echo message="The result code is ${ret.code} and it's succ ..." />
				<echo message="The testcase ${test.case.name}.js end with ${testcase.end.time} and cost ${testcase.timeOutput} s in total..." />

				<echo file="${reports.path}/TEST-${test.suite.class.name}.xml" append="true" message="&lt;testcase classname=&quot;${test.suite.class.name}&quot; name=&quot;${test.case.name}&quot; begin_time=&quot;${testcase.begin.time}&quot; end_time=&quot;${testcase.end.time}&quot; time=&quot;${testcase.timeOutput}&quot; path=&quot;${testcases.path}&quot; status=&quot;succ&quot;&gt;&#10;&lt;/testcase&gt;&#10;" />
			</else>
		</if>
		<echo message="********************************************"/>
	</target>
</project>
