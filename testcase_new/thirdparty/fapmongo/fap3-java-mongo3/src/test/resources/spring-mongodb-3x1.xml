<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xsi:schemaLocation="http://www.springframework.org/schema/context
          http://www.springframework.org/schema/context/spring-context-4.3.xsd
          http://www.springframework.org/schema/data/mongo
          http://www.springframework.org/schema/data/mongo/spring-mongo-1.10.xsd
          http://www.springframework.org/schema/beans
          http://www.springframework.org/schema/beans/spring-beans-4.3.xsd">

    <context:property-placeholder location="classpath*:mongodb.properties"/>
    <context:component-scan base-package="com.mongodb.*"/>

    <!-- spring连接mongodb数据库的配置 -->
    <mongo:mongo-client replica-set="${mongodb.singleUrl}" id="mongo">
        <mongo:client-options
                connections-per-host="${mongodb.connectionsPerHost}"
                threads-allowed-to-block-for-connection-multiplier="${mongodb.threadsAllowedToBlockForConnectionMultiplier}"
                connect-timeout="${mongodb.connectTimeout}"
                max-wait-time="${mongodb.maxWaitTime}"
                socket-keep-alive="${mongodb.socketKeepAlive}"
                socket-timeout="${mongodb.socketTimeout}"/>
    </mongo:mongo-client>

    <mongo:db-factory id="mongoDbFactory" dbname="${mongodb.springDBName}_${mongodb.springMongoVersion}" mongo-ref="mongo"/>

    <!-- 只要使用这个调用相应的方法操作 -->
    <bean id="mongoTemplate"
          class="org.springframework.data.mongodb.core.MongoTemplate">
        <constructor-arg name="mongoDbFactory" ref="mongoDbFactory"/>
    </bean>

    <bean id="config" class="com.mongodb.utils.Config" lazy-init="true">
        <constructor-arg type="String" value="${mongodb.javaMongoVersion}"/>
        <constructor-arg type="String" value="${mongodb.springMongoVersion}"/>
        <constructor-arg type="String" value="${mongodb.multiUrl}"/>
        <constructor-arg type="String" value="${mongodb.javaDBName}"/>
        <constructor-arg type="String" value="${mongodb.springDBName}"/>
        <constructor-arg type="String" value="${mongodb.username}"/>
        <constructor-arg type="String" value="${mongodb.password}"/>
    </bean>

</beans>