stpstop 是一个用于停止 STP 节点的工具。

## 权限需求

无

## 连接需求

无

## 选项

| 参数名 | 缩写 | 描述 |
| ---- | ---- | ---- |
| --help | -h | 返回 `stpstop` 的用法和帮助 |
| --version | | 返回 `stpstop` 的版本信息 |
| --force | | 强制停止 STP 节点 |

## 示例

通过 `stpstop` 停止 STP 节点

```lang-bash
$ bin/stpstop
```
